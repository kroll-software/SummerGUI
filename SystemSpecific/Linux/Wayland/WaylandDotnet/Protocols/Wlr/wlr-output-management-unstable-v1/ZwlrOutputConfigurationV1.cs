// <auto-generated>
// This file was generated from wlr-output-management-unstable-v1.xml
// Stability: Wlr
// Link: https://wayland.app/protocols/wlr-output-management-unstable-v1
// </auto-generated>

#nullable enable

namespace WaylandDotnet.Wlr;

using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;
using System.Runtime.InteropServices.Marshalling;
using System.Text;
using WaylandDotnet;
using WaylandDotnet.Internal;
using WaylandDotnet.Stable;
using WaylandDotnet.Staging;
using WaylandDotnet.Unstable;
using WaylandDotnet.Wlr;

/// <summary>
/// zwlr_output_configuration_v1
/// <para> output configuration </para>
/// <para> Version: 4 </para>
/// <see>https://wayland.app/protocols/wlr-output-management-unstable-v1/#zwlr_output_configuration_v1</see>
/// </summary>
public sealed partial class ZwlrOutputConfigurationV1 : WaylandObject, IWaylandObjectFactory<ZwlrOutputConfigurationV1>
{
    /// <summary> Wayland interface name for zwlr_output_configuration_v1. </summary>
    public const string InterfaceName = "zwlr_output_configuration_v1";
    /// <summary> Static interface name used by <see cref="IWaylandObjectFactory{T}"/>. </summary>
    public static string _StaticInterfaceName => "zwlr_output_configuration_v1";
    /// <summary> Interface version supported by this binding. </summary>
    public const int InterfaceVersion = 4;

    private bool disposed;

    private GCHandle gcHandle;
    private bool dispatcherRegistered = false;
    private readonly object dispatcherLock = new object();

    /// <summary> The display connection that owns this object. </summary>
    public new WlDisplay Display { get; private set; }

    /// <summary>
    /// Wraps an existing zwlr_output_configuration_v1 proxy handle.
    /// </summary>
    /// <param name="handle">The native Wayland proxy handle.</param>
    /// <param name="display">The display connection that owns this object.</param>
    public ZwlrOutputConfigurationV1(IntPtr handle, WlDisplay display)
    {
        Display = display;
        Handle = handle;
    }
    /// <summary>  </summary>
    public enum Error : uint
    {
        /// <summary>
        /// head has been configured twice
        /// </summary>
        AlreadyConfiguredHead = 1,
        /// <summary>
        /// head has not been configured
        /// </summary>
        UnconfiguredHead = 2,
        /// <summary>
        /// request sent after configuration has been applied or tested
        /// </summary>
        AlreadyUsed = 3,
    }

    /// <summary>
    /// Configuration changes succeeded
    /// <para>
    ///
    /// Sent after the compositor has successfully applied the changes or
    /// tested them.
    ///
    /// Upon receiving this event, the client should destroy this object.
    ///
    /// If the current configuration has changed, events to describe the changes
    /// will be sent followed by a wlr_output_manager.done event.
    /// 
    /// </para>
    /// </summary>
    public delegate void SucceededHandler();

    private SucceededHandler? _onSucceeded;

    /// <summary>
    /// Configuration changes succeeded
    /// <para>
    ///
    /// Sent after the compositor has successfully applied the changes or
    /// tested them.
    ///
    /// Upon receiving this event, the client should destroy this object.
    ///
    /// If the current configuration has changed, events to describe the changes
    /// will be sent followed by a wlr_output_manager.done event.
    /// 
    /// </para>
    /// </summary>
    public event SucceededHandler? OnSucceeded
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onSucceeded += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onSucceeded -= value;
        }
    }

    /// <summary>
    /// Configuration changes failed
    /// <para>
    ///
    /// Sent if the compositor rejects the changes or failed to apply them. The
    /// compositor should revert any changes made by the apply request that
    /// triggered this event.
    ///
    /// Upon receiving this event, the client should destroy this object.
    /// 
    /// </para>
    /// </summary>
    public delegate void FailedHandler();

    private FailedHandler? _onFailed;

    /// <summary>
    /// Configuration changes failed
    /// <para>
    ///
    /// Sent if the compositor rejects the changes or failed to apply them. The
    /// compositor should revert any changes made by the apply request that
    /// triggered this event.
    ///
    /// Upon receiving this event, the client should destroy this object.
    /// 
    /// </para>
    /// </summary>
    public event FailedHandler? OnFailed
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onFailed += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onFailed -= value;
        }
    }

    /// <summary>
    /// Configuration has been cancelled
    /// <para>
    ///
    /// Sent if the compositor cancels the configuration because the state of an
    /// output changed and the client has outdated information (e.g. after an
    /// output has been hotplugged).
    ///
    /// The client can create a new configuration with a newer serial and try
    /// again.
    ///
    /// Upon receiving this event, the client should destroy this object.
    /// 
    /// </para>
    /// </summary>
    public delegate void CancelledHandler();

    private CancelledHandler? _onCancelled;

    /// <summary>
    /// Configuration has been cancelled
    /// <para>
    ///
    /// Sent if the compositor cancels the configuration because the state of an
    /// output changed and the client has outdated information (e.g. after an
    /// output has been hotplugged).
    ///
    /// The client can create a new configuration with a newer serial and try
    /// again.
    ///
    /// Upon receiving this event, the client should destroy this object.
    /// 
    /// </para>
    /// </summary>
    public event CancelledHandler? OnCancelled
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onCancelled += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onCancelled -= value;
        }
    }

    private unsafe void EnsureDispatcherRegistered()
    {
        lock (dispatcherLock)
        {
            if (dispatcherRegistered)
            {
                return;
            }

            // Allocate GCHandle to keep this object alive
            gcHandle = GCHandle.Alloc(this, GCHandleType.Normal);

            // Register the dispatcher with the Wayland proxy
            var dataPtr = GCHandle.ToIntPtr(gcHandle);
            var result = WaylandNative.ProxyAddDispatcher(Handle, &DispatchEvent, dataPtr, IntPtr.Zero);
            if (result != 0)
            {
                gcHandle.Free();
                throw new InvalidOperationException("Failed to add dispatcher to zwlr_output_configuration_v1");
            }

            dispatcherRegistered = true;
        }
    }

    [UnmanagedCallersOnly]
    private static unsafe int DispatchEvent(IntPtr userData, IntPtr target, uint opcode, WlMessage* message, WlArgument* args)
    {
        try
        {
            var handle = GCHandle.FromIntPtr(userData);
            var obj = (ZwlrOutputConfigurationV1)handle.Target!;

            switch (opcode)
            {
                case 0: // succeeded
                    if (obj._onSucceeded != null)
                    {
                        obj._onSucceeded?.Invoke();
                    }
                    break;
                case 1: // failed
                    if (obj._onFailed != null)
                    {
                        obj._onFailed?.Invoke();
                    }
                    break;
                case 2: // cancelled
                    if (obj._onCancelled != null)
                    {
                        obj._onCancelled?.Invoke();
                    }
                    break;
                default:
                    return -1;
            }

            return 0;
        }
        catch (Exception ex)
        {
            Console.Error.WriteLine($"Error in event dispatcher: {ex}");
            return -1;
        }
    }
    /// <summary>
    /// Enable and configure a head
    /// <para>
    /// <br/>
    /// Enable a head. This request creates a head configuration object that can<br/>
    /// be used to change the head's properties.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe ZwlrOutputConfigurationHeadV1 EnableHead(ZwlrOutputHeadV1 head)
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[2];
        args[0].o = (WlObject*)IntPtr.Zero;
        args[1].o = (WlObject*)(head?.Handle ?? IntPtr.Zero);

        const uint opcode = 0;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            WaylandInterfaces.ZwlrOutputConfigurationHeadV1,
            (uint)WaylandInterfaces.ZwlrOutputConfigurationHeadV1->Version,
            0,
            (nint)args
        );

        return new ZwlrOutputConfigurationHeadV1(newProxy);
    }

    /// <summary>
    /// Disable a head
    /// <para>
    /// <br/>
    /// Disable a head.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void DisableHead(ZwlrOutputHeadV1 head)
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[1];
        args[0].o = (WlObject*)(head?.Handle ?? IntPtr.Zero);

        const uint opcode = 1;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Apply the configuration
    /// <para>
    /// <br/>
    /// Apply the new output configuration.<br/>
    /// <br/>
    /// In case the configuration is successfully applied, there is no guarantee<br/>
    /// that the new output state matches completely the requested<br/>
    /// configuration. For instance, a compositor might round the scale if it<br/>
    /// doesn't support fractional scaling.<br/>
    /// <br/>
    /// After this request has been sent, the compositor must respond with an<br/>
    /// succeeded, failed or cancelled event. Sending a request that isn't the<br/>
    /// destructor is a protocol error.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void Apply()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 2;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Test the configuration
    /// <para>
    /// <br/>
    /// Test the new output configuration. The configuration won't be applied,<br/>
    /// but will only be validated.<br/>
    /// <br/>
    /// Even if the compositor succeeds to test a configuration, applying it may<br/>
    /// fail.<br/>
    /// <br/>
    /// After this request has been sent, the compositor must respond with an<br/>
    /// succeeded, failed or cancelled event. Sending a request that isn't the<br/>
    /// destructor is a protocol error.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void Test()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 3;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Destroy the output configuration
    /// <para>
    /// <br/>
    /// Using this request a client can tell the compositor that it is not going<br/>
    /// to use the configuration object anymore. Any changes to the outputs<br/>
    /// that have not been applied will be discarded.<br/>
    /// <br/>
    /// This request also destroys wlr_output_configuration_head objects created<br/>
    /// via this object.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void Destroy()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 4;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
        disposed = true;
    }

    /// <summary> Creates a ZwlrOutputConfigurationV1 wrapper from an existing proxy handle. </summary>
    /// <param name="handle">The native Wayland proxy handle.</param>
    /// <param name="display">The display connection that owns this object, when required.</param>
    /// <returns>A new ZwlrOutputConfigurationV1 instance.</returns>
    public static ZwlrOutputConfigurationV1 Create(nint handle, WlDisplay? display = null)
    {
        ArgumentNullException.ThrowIfNull(display);
        return new ZwlrOutputConfigurationV1(handle, display);
    }
}
