// <auto-generated>
// This file was generated from wlr-foreign-toplevel-management-unstable-v1.xml
// Stability: Wlr
// Link: https://wayland.app/protocols/wlr-foreign-toplevel-management-unstable-v1
// </auto-generated>

#nullable enable

namespace WaylandDotnet.Wlr;

using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;
using System.Runtime.InteropServices.Marshalling;
using System.Text;
using WaylandDotnet;
using WaylandDotnet.Internal;
using WaylandDotnet.Stable;
using WaylandDotnet.Staging;
using WaylandDotnet.Unstable;
using WaylandDotnet.Wlr;

/// <summary>
/// zwlr_foreign_toplevel_handle_v1
/// <para> an opened toplevel </para>
/// <para> Version: 3 </para>
/// <see>https://wayland.app/protocols/wlr-foreign-toplevel-management-unstable-v1/#zwlr_foreign_toplevel_handle_v1</see>
/// </summary>
public sealed partial class ZwlrForeignToplevelHandleV1 : WaylandObject, IWaylandObjectFactory<ZwlrForeignToplevelHandleV1>
{
    /// <summary> Wayland interface name for zwlr_foreign_toplevel_handle_v1. </summary>
    public const string InterfaceName = "zwlr_foreign_toplevel_handle_v1";
    /// <summary> Static interface name used by <see cref="IWaylandObjectFactory{T}"/>. </summary>
    public static string _StaticInterfaceName => "zwlr_foreign_toplevel_handle_v1";
    /// <summary> Interface version supported by this binding. </summary>
    public const int InterfaceVersion = 3;

    private bool disposed;

    private GCHandle gcHandle;
    private bool dispatcherRegistered = false;
    private readonly object dispatcherLock = new object();

    /// <summary> The display connection that owns this object. </summary>
    public new WlDisplay Display { get; private set; }

    /// <summary>
    /// Wraps an existing zwlr_foreign_toplevel_handle_v1 proxy handle.
    /// </summary>
    /// <param name="handle">The native Wayland proxy handle.</param>
    /// <param name="display">The display connection that owns this object.</param>
    public ZwlrForeignToplevelHandleV1(IntPtr handle, WlDisplay display)
    {
        Display = display;
        Handle = handle;
    }
    /// <summary> types of states on the toplevel </summary>
    public enum State : uint
    {
        /// <summary>
        /// the toplevel is maximized
        /// </summary>
        Maximized = 0,
        /// <summary>
        /// the toplevel is minimized
        /// </summary>
        Minimized = 1,
        /// <summary>
        /// the toplevel is active
        /// </summary>
        Activated = 2,
        /// <summary>
        /// the toplevel is fullscreen
        /// </summary>
        Fullscreen = 3,
    }

    /// <summary>  </summary>
    public enum Error : uint
    {
        /// <summary>
        /// the provided rectangle is invalid
        /// </summary>
        InvalidRectangle = 0,
    }

    /// <summary>
    /// Title change
    /// <para>
    ///
    /// This event is emitted whenever the title of the toplevel changes.
    /// 
    /// </para>
    /// </summary>
    public delegate void TitleHandler(string title);

    private TitleHandler? _onTitle;

    /// <summary>
    /// Title change
    /// <para>
    ///
    /// This event is emitted whenever the title of the toplevel changes.
    /// 
    /// </para>
    /// </summary>
    public event TitleHandler? OnTitle
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onTitle += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onTitle -= value;
        }
    }

    /// <summary>
    /// App-id change
    /// <para>
    ///
    /// This event is emitted whenever the app-id of the toplevel changes.
    /// 
    /// </para>
    /// </summary>
    public delegate void AppIdHandler(string appId);

    private AppIdHandler? _onAppId;

    /// <summary>
    /// App-id change
    /// <para>
    ///
    /// This event is emitted whenever the app-id of the toplevel changes.
    /// 
    /// </para>
    /// </summary>
    public event AppIdHandler? OnAppId
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onAppId += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onAppId -= value;
        }
    }

    /// <summary>
    /// Toplevel entered an output
    /// <para>
    ///
    /// This event is emitted whenever the toplevel becomes visible on
    /// the given output. A toplevel may be visible on multiple outputs.
    /// 
    /// </para>
    /// </summary>
    public delegate void OutputEnterHandler(WlOutput output);

    private OutputEnterHandler? _onOutputEnter;

    /// <summary>
    /// Toplevel entered an output
    /// <para>
    ///
    /// This event is emitted whenever the toplevel becomes visible on
    /// the given output. A toplevel may be visible on multiple outputs.
    /// 
    /// </para>
    /// </summary>
    public event OutputEnterHandler? OnOutputEnter
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onOutputEnter += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onOutputEnter -= value;
        }
    }

    /// <summary>
    /// Toplevel left an output
    /// <para>
    ///
    /// This event is emitted whenever the toplevel stops being visible on
    /// the given output. It is guaranteed that an entered-output event
    /// with the same output has been emitted before this event.
    /// 
    /// </para>
    /// </summary>
    public delegate void OutputLeaveHandler(WlOutput output);

    private OutputLeaveHandler? _onOutputLeave;

    /// <summary>
    /// Toplevel left an output
    /// <para>
    ///
    /// This event is emitted whenever the toplevel stops being visible on
    /// the given output. It is guaranteed that an entered-output event
    /// with the same output has been emitted before this event.
    /// 
    /// </para>
    /// </summary>
    public event OutputLeaveHandler? OnOutputLeave
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onOutputLeave += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onOutputLeave -= value;
        }
    }

    /// <summary>
    /// The toplevel state changed
    /// <para>
    ///
    /// This event is emitted immediately after the zlw_foreign_toplevel_handle_v1
    /// is created and each time the toplevel state changes, either because of a
    /// compositor action or because of a request in this protocol.
    /// 
    /// </para>
    /// </summary>
    public delegate void StateHandler(byte[] state);

    private StateHandler? _onState;

    /// <summary>
    /// The toplevel state changed
    /// <para>
    ///
    /// This event is emitted immediately after the zlw_foreign_toplevel_handle_v1
    /// is created and each time the toplevel state changes, either because of a
    /// compositor action or because of a request in this protocol.
    /// 
    /// </para>
    /// </summary>
    public event StateHandler? OnState
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onState += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onState -= value;
        }
    }

    /// <summary>
    /// All information about the toplevel has been sent
    /// <para>
    ///
    /// This event is sent after all changes in the toplevel state have been
    /// sent.
    ///
    /// This allows changes to the zwlr_foreign_toplevel_handle_v1 properties
    /// to be seen as atomic, even if they happen via multiple events.
    /// 
    /// </para>
    /// </summary>
    public delegate void DoneHandler();

    private DoneHandler? _onDone;

    /// <summary>
    /// All information about the toplevel has been sent
    /// <para>
    ///
    /// This event is sent after all changes in the toplevel state have been
    /// sent.
    ///
    /// This allows changes to the zwlr_foreign_toplevel_handle_v1 properties
    /// to be seen as atomic, even if they happen via multiple events.
    /// 
    /// </para>
    /// </summary>
    public event DoneHandler? OnDone
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onDone += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onDone -= value;
        }
    }

    /// <summary>
    /// This toplevel has been destroyed
    /// <para>
    ///
    /// This event means the toplevel has been destroyed. It is guaranteed there
    /// won't be any more events for this zwlr_foreign_toplevel_handle_v1. The
    /// toplevel itself becomes inert so any requests will be ignored except the
    /// destroy request.
    /// 
    /// </para>
    /// </summary>
    public delegate void ClosedHandler();

    private ClosedHandler? _onClosed;

    /// <summary>
    /// This toplevel has been destroyed
    /// <para>
    ///
    /// This event means the toplevel has been destroyed. It is guaranteed there
    /// won't be any more events for this zwlr_foreign_toplevel_handle_v1. The
    /// toplevel itself becomes inert so any requests will be ignored except the
    /// destroy request.
    /// 
    /// </para>
    /// </summary>
    public event ClosedHandler? OnClosed
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onClosed += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onClosed -= value;
        }
    }

    /// <summary>
    /// Parent change
    /// <para>
    ///
    /// This event is emitted whenever the parent of the toplevel changes.
    ///
    /// No event is emitted when the parent handle is destroyed by the client.
    /// 
    /// </para>
    /// </summary>
    public delegate void ParentHandler(ZwlrForeignToplevelHandleV1? parent);

    private ParentHandler? _onParent;

    /// <summary>
    /// Parent change
    /// <para>
    ///
    /// This event is emitted whenever the parent of the toplevel changes.
    ///
    /// No event is emitted when the parent handle is destroyed by the client.
    /// 
    /// </para>
    /// </summary>
    public event ParentHandler? OnParent
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onParent += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onParent -= value;
        }
    }

    private unsafe void EnsureDispatcherRegistered()
    {
        lock (dispatcherLock)
        {
            if (dispatcherRegistered)
            {
                return;
            }

            // Allocate GCHandle to keep this object alive
            gcHandle = GCHandle.Alloc(this, GCHandleType.Normal);

            // Register the dispatcher with the Wayland proxy
            var dataPtr = GCHandle.ToIntPtr(gcHandle);
            var result = WaylandNative.ProxyAddDispatcher(Handle, &DispatchEvent, dataPtr, IntPtr.Zero);
            if (result != 0)
            {
                gcHandle.Free();
                throw new InvalidOperationException("Failed to add dispatcher to zwlr_foreign_toplevel_handle_v1");
            }

            dispatcherRegistered = true;
        }
    }

    [UnmanagedCallersOnly]
    private static unsafe int DispatchEvent(IntPtr userData, IntPtr target, uint opcode, WlMessage* message, WlArgument* args)
    {
        try
        {
            var handle = GCHandle.FromIntPtr(userData);
            var obj = (ZwlrForeignToplevelHandleV1)handle.Target!;

            switch (opcode)
            {
                case 0: // title
                    if (obj._onTitle != null)
                    {
                        var _title = Utf8StringMarshaller.ConvertToManaged(args[0].s) ?? string.Empty;
                        obj._onTitle?.Invoke(_title);
                    }
                    break;
                case 1: // app_id
                    if (obj._onAppId != null)
                    {
                        var _appId = Utf8StringMarshaller.ConvertToManaged(args[0].s) ?? string.Empty;
                        obj._onAppId?.Invoke(_appId);
                    }
                    break;
                case 2: // output_enter
                    if (obj._onOutputEnter != null)
                    {
                        if (args[0].o == (WlObject*)IntPtr.Zero) throw new InvalidOperationException("Received null object for non-nullable argument 'output'");
                        var _output = new WlOutput((IntPtr)args[0].o, obj.Display!);
                        obj._onOutputEnter?.Invoke(_output);
                    }
                    break;
                case 3: // output_leave
                    if (obj._onOutputLeave != null)
                    {
                        if (args[0].o == (WlObject*)IntPtr.Zero) throw new InvalidOperationException("Received null object for non-nullable argument 'output'");
                        var _output = new WlOutput((IntPtr)args[0].o, obj.Display!);
                        obj._onOutputLeave?.Invoke(_output);
                    }
                    break;
                case 4: // state
                    if (obj._onState != null)
                    {
                        var _state = args[0].a;
                        obj._onState?.Invoke(WaylandMarshal.ToSpan(_state));
                    }
                    break;
                case 5: // done
                    if (obj._onDone != null)
                    {
                        obj._onDone?.Invoke();
                    }
                    break;
                case 6: // closed
                    if (obj._onClosed != null)
                    {
                        obj._onClosed?.Invoke();
                    }
                    break;
                case 7: // parent
                    if (obj._onParent != null)
                    {
                        ZwlrForeignToplevelHandleV1? _parent = null;
                        if (args[0].o != (WlObject*)IntPtr.Zero)
                        {
                            _parent = new ZwlrForeignToplevelHandleV1((IntPtr)args[0].o, obj.Display!);
                        }
                        obj._onParent?.Invoke(_parent);
                    }
                    break;
                default:
                    return -1;
            }

            return 0;
        }
        catch (Exception ex)
        {
            Console.Error.WriteLine($"Error in event dispatcher: {ex}");
            return -1;
        }
    }
    /// <summary>
    /// Requests that the toplevel be maximized
    /// <para>
    /// <br/>
    /// Requests that the toplevel be maximized. If the maximized state actually<br/>
    /// changes, this will be indicated by the state event.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void SetMaximized()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 0;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Requests that the toplevel be unmaximized
    /// <para>
    /// <br/>
    /// Requests that the toplevel be unmaximized. If the maximized state actually<br/>
    /// changes, this will be indicated by the state event.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void UnsetMaximized()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 1;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Requests that the toplevel be minimized
    /// <para>
    /// <br/>
    /// Requests that the toplevel be minimized. If the minimized state actually<br/>
    /// changes, this will be indicated by the state event.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void SetMinimized()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 2;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Requests that the toplevel be unminimized
    /// <para>
    /// <br/>
    /// Requests that the toplevel be unminimized. If the minimized state actually<br/>
    /// changes, this will be indicated by the state event.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void UnsetMinimized()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 3;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Activate the toplevel
    /// <para>
    /// <br/>
    /// Request that this toplevel be activated on the given seat.<br/>
    /// There is no guarantee the toplevel will be actually activated.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void Activate(WlSeat seat)
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[1];
        args[0].o = (WlObject*)(seat?.Handle ?? IntPtr.Zero);

        const uint opcode = 4;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Request that the toplevel be closed
    /// <para>
    /// <br/>
    /// Send a request to the toplevel to close itself. The compositor would<br/>
    /// typically use a shell-specific method to carry out this request, for<br/>
    /// example by sending the xdg_toplevel.close event. However, this gives<br/>
    /// no guarantees the toplevel will actually be destroyed. If and when<br/>
    /// this happens, the zwlr_foreign_toplevel_handle_v1.closed event will<br/>
    /// be emitted.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void Close()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 5;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// The rectangle which represents the toplevel
    /// <para>
    /// <br/>
    /// The rectangle of the surface specified in this request corresponds to<br/>
    /// the place where the app using this protocol represents the given toplevel.<br/>
    /// It can be used by the compositor as a hint for some operations, e.g<br/>
    /// minimizing. The client is however not required to set this, in which<br/>
    /// case the compositor is free to decide some default value.<br/>
    /// <br/>
    /// If the client specifies more than one rectangle, only the last one is<br/>
    /// considered.<br/>
    /// <br/>
    /// The dimensions are given in surface-local coordinates.<br/>
    /// Setting width=height=0 removes the already-set rectangle.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void SetRectangle(WlSurface surface, int x, int y, int width, int height)
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[5];
        args[0].o = (WlObject*)(surface?.Handle ?? IntPtr.Zero);
        args[1].i = x;
        args[2].i = y;
        args[3].i = width;
        args[4].i = height;

        const uint opcode = 6;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Destroy the zwlr_foreign_toplevel_handle_v1 object
    /// <para>
    /// <br/>
    /// Destroys the zwlr_foreign_toplevel_handle_v1 object.<br/>
    /// <br/>
    /// This request should be called either when the client does not want to<br/>
    /// use the toplevel anymore or after the closed event to finalize the<br/>
    /// destruction of the object.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void Destroy()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 7;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
        disposed = true;
    }

    /// <summary>
    /// Request that the toplevel be fullscreened
    /// <para>
    /// <br/>
    /// Requests that the toplevel be fullscreened on the given output. If the<br/>
    /// fullscreen state and/or the outputs the toplevel is visible on actually<br/>
    /// change, this will be indicated by the state and output_enter/leave<br/>
    /// events.<br/>
    /// <br/>
    /// The output parameter is only a hint to the compositor. Also, if output<br/>
    /// is NULL, the compositor should decide which output the toplevel will be<br/>
    /// fullscreened on, if at all.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void SetFullscreen(WlOutput? output)
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[1];
        args[0].o = (WlObject*)(output?.Handle ?? IntPtr.Zero);

        const uint opcode = 8;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Request that the toplevel be unfullscreened
    /// <para>
    /// <br/>
    /// Requests that the toplevel be unfullscreened. If the fullscreen state<br/>
    /// actually changes, this will be indicated by the state event.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void UnsetFullscreen()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 9;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary> Creates a ZwlrForeignToplevelHandleV1 wrapper from an existing proxy handle. </summary>
    /// <param name="handle">The native Wayland proxy handle.</param>
    /// <param name="display">The display connection that owns this object, when required.</param>
    /// <returns>A new ZwlrForeignToplevelHandleV1 instance.</returns>
    public static ZwlrForeignToplevelHandleV1 Create(nint handle, WlDisplay? display = null)
    {
        ArgumentNullException.ThrowIfNull(display);
        return new ZwlrForeignToplevelHandleV1(handle, display);
    }
}
