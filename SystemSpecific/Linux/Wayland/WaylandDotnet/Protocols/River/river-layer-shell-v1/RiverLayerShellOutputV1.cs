// <auto-generated>
// This file was generated from river-layer-shell-v1.xml
// Stability: River
// Link: https://wayland.app/protocols/river-layer-shell-v1
// </auto-generated>

#nullable enable

namespace WaylandDotnet.River;

using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;
using System.Runtime.InteropServices.Marshalling;
using System.Text;
using WaylandDotnet;
using WaylandDotnet.Internal;
using WaylandDotnet.Stable;
using WaylandDotnet.Staging;
using WaylandDotnet.Unstable;
using WaylandDotnet.Wlr;

/// <summary>
/// river_layer_shell_output_v1
/// <para> layer shell output state </para>
/// <para> Version: 1 </para>
/// <see>https://wayland.app/protocols/river-layer-shell-v1/#river_layer_shell_output_v1</see>
/// </summary>
public sealed partial class RiverLayerShellOutputV1 : WaylandObject, IWaylandObjectFactory<RiverLayerShellOutputV1>
{
    /// <summary> Wayland interface name for river_layer_shell_output_v1. </summary>
    public const string InterfaceName = "river_layer_shell_output_v1";
    /// <summary> Static interface name used by <see cref="IWaylandObjectFactory{T}"/>. </summary>
    public static string _StaticInterfaceName => "river_layer_shell_output_v1";
    /// <summary> Interface version supported by this binding. </summary>
    public const int InterfaceVersion = 1;

    private bool disposed;

    private GCHandle gcHandle;
    private bool dispatcherRegistered = false;
    private readonly object dispatcherLock = new object();

    /// <summary> The display connection that owns this object. </summary>
    public new WlDisplay Display { get; private set; }

    /// <summary>
    /// Wraps an existing river_layer_shell_output_v1 proxy handle.
    /// </summary>
    /// <param name="handle">The native Wayland proxy handle.</param>
    /// <param name="display">The display connection that owns this object.</param>
    public RiverLayerShellOutputV1(IntPtr handle, WlDisplay display)
    {
        Display = display;
        Handle = handle;
    }
    /// <summary>
    /// Area left after subtracting exclusive zones
    /// <para>
    ///
    /// This event indicates the area of the output remaining after subtracting
    /// the exclusive zones of layer surfaces. Exclusive zones are a hint, the
    /// window manager is free to ignore this area hint if it wishes.
    ///
    /// The x and y values are in the global coordinate space, not relative to
    /// the position of the output.
    ///
    /// This event will be followed by a manage_start event after all other new
    /// state has been sent by the server.
    /// 
    /// </para>
    /// </summary>
    public delegate void NonExclusiveAreaHandler(int x, int y, int width, int height);

    private NonExclusiveAreaHandler? _onNonExclusiveArea;

    /// <summary>
    /// Area left after subtracting exclusive zones
    /// <para>
    ///
    /// This event indicates the area of the output remaining after subtracting
    /// the exclusive zones of layer surfaces. Exclusive zones are a hint, the
    /// window manager is free to ignore this area hint if it wishes.
    ///
    /// The x and y values are in the global coordinate space, not relative to
    /// the position of the output.
    ///
    /// This event will be followed by a manage_start event after all other new
    /// state has been sent by the server.
    /// 
    /// </para>
    /// </summary>
    public event NonExclusiveAreaHandler? OnNonExclusiveArea
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onNonExclusiveArea += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onNonExclusiveArea -= value;
        }
    }

    private unsafe void EnsureDispatcherRegistered()
    {
        lock (dispatcherLock)
        {
            if (dispatcherRegistered)
            {
                return;
            }

            // Allocate GCHandle to keep this object alive
            gcHandle = GCHandle.Alloc(this, GCHandleType.Normal);

            // Register the dispatcher with the Wayland proxy
            var dataPtr = GCHandle.ToIntPtr(gcHandle);
            var result = WaylandNative.ProxyAddDispatcher(Handle, &DispatchEvent, dataPtr, IntPtr.Zero);
            if (result != 0)
            {
                gcHandle.Free();
                throw new InvalidOperationException("Failed to add dispatcher to river_layer_shell_output_v1");
            }

            dispatcherRegistered = true;
        }
    }

    [UnmanagedCallersOnly]
    private static unsafe int DispatchEvent(IntPtr userData, IntPtr target, uint opcode, WlMessage* message, WlArgument* args)
    {
        try
        {
            var handle = GCHandle.FromIntPtr(userData);
            var obj = (RiverLayerShellOutputV1)handle.Target!;

            switch (opcode)
            {
                case 0: // non_exclusive_area
                    if (obj._onNonExclusiveArea != null)
                    {
                        var _x = args[0].i;
                        var _y = args[1].i;
                        var _width = args[2].i;
                        var _height = args[3].i;
                        obj._onNonExclusiveArea?.Invoke(_x, _y, _width, _height);
                    }
                    break;
                default:
                    return -1;
            }

            return 0;
        }
        catch (Exception ex)
        {
            Console.Error.WriteLine($"Error in event dispatcher: {ex}");
            return -1;
        }
    }
    /// <summary>
    /// Destroy the object
    /// <para>
    /// <br/>
    /// This request indicates that the client will no longer use the<br/>
    /// river_layer_shell_output_v1 object and that it may be safely destroyed.<br/>
    /// <br/>
    /// This request should be made after the river_output_v1.removed event is<br/>
    /// received to complete destruction of the output.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void Destroy()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 0;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
        disposed = true;
    }

    /// <summary>
    /// Set default output for layer surfaces
    /// <para>
    /// <br/>
    /// Mark this output as the default for new layer surfaces which do not<br/>
    /// request a specific output themselves. This request overrides any<br/>
    /// previous set_default request on any river_layer_shell_output_v1 object.<br/>
    /// <br/>
    /// If no set_default request is made or if the default output is destroyed,<br/>
    /// the default output is undefined until the next set_default request.<br/>
    /// <br/>
    /// This request modifies window management state and may only be made as<br/>
    /// part of a manage sequence, see the river_window_manager_v1 description.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void SetDefault()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 1;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary> Creates a RiverLayerShellOutputV1 wrapper from an existing proxy handle. </summary>
    /// <param name="handle">The native Wayland proxy handle.</param>
    /// <param name="display">The display connection that owns this object, when required.</param>
    /// <returns>A new RiverLayerShellOutputV1 instance.</returns>
    public static RiverLayerShellOutputV1 Create(nint handle, WlDisplay? display = null)
    {
        ArgumentNullException.ThrowIfNull(display);
        return new RiverLayerShellOutputV1(handle, display);
    }
}
