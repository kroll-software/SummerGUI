// <auto-generated>
// This file was generated from river-xkb-config-v1.xml
// Stability: River
// Link: https://wayland.app/protocols/river-xkb-config-v1
// </auto-generated>

#nullable enable

namespace WaylandDotnet.River;

using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;
using System.Runtime.InteropServices.Marshalling;
using System.Text;
using WaylandDotnet;
using WaylandDotnet.Internal;
using WaylandDotnet.Stable;
using WaylandDotnet.Staging;
using WaylandDotnet.Unstable;
using WaylandDotnet.Wlr;

/// <summary>
/// river_xkb_config_v1
/// <para> xkb config global interface </para>
/// <para> Version: 2 </para>
/// <see>https://wayland.app/protocols/river-xkb-config-v1/#river_xkb_config_v1</see>
/// </summary>
public sealed partial class RiverXkbConfigV1 : WaylandObject, IWaylandObjectFactory<RiverXkbConfigV1>
{
    /// <summary> Wayland interface name for river_xkb_config_v1. </summary>
    public const string InterfaceName = "river_xkb_config_v1";
    /// <summary> Static interface name used by <see cref="IWaylandObjectFactory{T}"/>. </summary>
    public static string _StaticInterfaceName => "river_xkb_config_v1";
    /// <summary> Interface version supported by this binding. </summary>
    public const int InterfaceVersion = 2;

    private bool disposed;

    private GCHandle gcHandle;
    private bool dispatcherRegistered = false;
    private readonly object dispatcherLock = new object();

    /// <summary> The display connection that owns this object. </summary>
    public new WlDisplay Display { get; private set; }

    /// <summary>
    /// Wraps an existing river_xkb_config_v1 proxy handle.
    /// </summary>
    /// <param name="handle">The native Wayland proxy handle.</param>
    /// <param name="display">The display connection that owns this object.</param>
    public RiverXkbConfigV1(IntPtr handle, WlDisplay display)
    {
        Display = display;
        Handle = handle;
    }
    /// <summary>  </summary>
    public enum Error : uint
    {
        /// <summary>
        ///
        /// </summary>
        InvalidDestroy = 0,
        /// <summary>
        ///
        /// </summary>
        InvalidFormat = 1,
    }

    /// <summary>  </summary>
    public enum KeymapFormat : uint
    {
        /// <summary>
        /// XKB_KEYMAP_FORMAT_TEXT_V1
        /// </summary>
        TextV1 = 1,
        /// <summary>
        /// XKB_KEYMAP_FORMAT_TEXT_V2
        /// </summary>
        TextV2 = 2,
    }

    /// <summary>
    /// The server has finished with the object
    /// <para>
    ///
    /// This event indicates that the server will send no further events on this
    /// object. The client should destroy the object. See
    /// river_xkb_config_v1.destroy for more information.
    /// 
    /// </para>
    /// </summary>
    public delegate void FinishedHandler();

    private FinishedHandler? _onFinished;

    /// <summary>
    /// The server has finished with the object
    /// <para>
    ///
    /// This event indicates that the server will send no further events on this
    /// object. The client should destroy the object. See
    /// river_xkb_config_v1.destroy for more information.
    /// 
    /// </para>
    /// </summary>
    public event FinishedHandler? OnFinished
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onFinished += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onFinished -= value;
        }
    }

    /// <summary>
    /// New xkb keyboard
    /// <para>
    ///
    /// A new xkbcommon keyboard has been created. Not every
    /// river_input_device_v1 is necessarily an xkbcommon keyboard as well.
    /// 
    /// </para>
    /// </summary>
    public delegate void XkbKeyboardHandler(RiverXkbKeyboardV1 id);

    private XkbKeyboardHandler? _onXkbKeyboard;

    /// <summary>
    /// New xkb keyboard
    /// <para>
    ///
    /// A new xkbcommon keyboard has been created. Not every
    /// river_input_device_v1 is necessarily an xkbcommon keyboard as well.
    /// 
    /// </para>
    /// </summary>
    public event XkbKeyboardHandler? OnXkbKeyboard
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onXkbKeyboard += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onXkbKeyboard -= value;
        }
    }

    private unsafe void EnsureDispatcherRegistered()
    {
        lock (dispatcherLock)
        {
            if (dispatcherRegistered)
            {
                return;
            }

            // Allocate GCHandle to keep this object alive
            gcHandle = GCHandle.Alloc(this, GCHandleType.Normal);

            // Register the dispatcher with the Wayland proxy
            var dataPtr = GCHandle.ToIntPtr(gcHandle);
            var result = WaylandNative.ProxyAddDispatcher(Handle, &DispatchEvent, dataPtr, IntPtr.Zero);
            if (result != 0)
            {
                gcHandle.Free();
                throw new InvalidOperationException("Failed to add dispatcher to river_xkb_config_v1");
            }

            dispatcherRegistered = true;
        }
    }

    [UnmanagedCallersOnly]
    private static unsafe int DispatchEvent(IntPtr userData, IntPtr target, uint opcode, WlMessage* message, WlArgument* args)
    {
        try
        {
            var handle = GCHandle.FromIntPtr(userData);
            var obj = (RiverXkbConfigV1)handle.Target!;

            switch (opcode)
            {
                case 0: // finished
                    if (obj._onFinished != null)
                    {
                        obj._onFinished?.Invoke();
                    }
                    break;
                case 1: // xkb_keyboard
                    if (obj._onXkbKeyboard != null)
                    {
                        if (args[0].o == (WlObject*)IntPtr.Zero) throw new InvalidOperationException("Received null object for non-nullable argument 'id'");
                        var _id = new RiverXkbKeyboardV1((IntPtr)args[0].o, obj.Display!);
                        obj._onXkbKeyboard?.Invoke(_id);
                    }
                    break;
                default:
                    return -1;
            }

            return 0;
        }
        catch (Exception ex)
        {
            Console.Error.WriteLine($"Error in event dispatcher: {ex}");
            return -1;
        }
    }
    /// <summary>
    /// Stop sending events
    /// <para>
    /// <br/>
    /// This request indicates that the client no longer wishes to receive<br/>
    /// events on this object.<br/>
    /// <br/>
    /// The Wayland protocol is asynchronous, which means the server may send<br/>
    /// further events until the stop request is processed. The client must wait<br/>
    /// for a river_xkb_config_v1.finished event before destroying this object.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void Stop()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 0;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Destroy the river_xkb_config_v1 object
    /// <para>
    /// <br/>
    /// This request should be called after the finished event has been received<br/>
    /// to complete destruction of the object.<br/>
    /// <br/>
    /// It is a protocol error to make this request before the finished event<br/>
    /// has been received.<br/>
    /// <br/>
    /// If a client wishes to destroy this object it should send a<br/>
    /// river_xkb_config_v1.stop request and wait for a<br/>
    /// river_xkb_config_v1.finished event. Once the finished event is received<br/>
    /// it is safe to destroy this object and any other objects created through<br/>
    /// this interface.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void Destroy()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 1;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
        disposed = true;
    }

    /// <summary>
    /// Create a keymap object
    /// <para>
    /// <br/>
    /// The server must be able to mmap the fd with MAP_PRIVATE.<br/>
    /// The server will fstat the fd to obtain the size of the keymap.<br/>
    /// The client must not modify the contents of the fd after making this request.<br/>
    /// The client should seal the fd with fcntl.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe RiverXkbKeymapV1 CreateKeymap(int fd, uint format)
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[3];
        args[0].o = (WlObject*)IntPtr.Zero;
        args[1].h = fd;
        args[2].u = format;

        const uint opcode = 2;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            WaylandInterfaces.RiverXkbKeymapV1,
            (uint)WaylandInterfaces.RiverXkbKeymapV1->Version,
            0,
            (nint)args
        );

        return new RiverXkbKeymapV1(newProxy, Display);
    }

    /// <summary> Creates a RiverXkbConfigV1 wrapper from an existing proxy handle. </summary>
    /// <param name="handle">The native Wayland proxy handle.</param>
    /// <param name="display">The display connection that owns this object, when required.</param>
    /// <returns>A new RiverXkbConfigV1 instance.</returns>
    public static RiverXkbConfigV1 Create(nint handle, WlDisplay? display = null)
    {
        ArgumentNullException.ThrowIfNull(display);
        return new RiverXkbConfigV1(handle, display);
    }
}
