// <auto-generated>
// This file was generated from river-xkb-config-v1.xml
// Stability: River
// Link: https://wayland.app/protocols/river-xkb-config-v1
// </auto-generated>

#nullable enable

namespace WaylandDotnet.River;

using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;
using System.Runtime.InteropServices.Marshalling;
using System.Text;
using WaylandDotnet;
using WaylandDotnet.Internal;
using WaylandDotnet.Stable;
using WaylandDotnet.Staging;
using WaylandDotnet.Unstable;
using WaylandDotnet.Wlr;

/// <summary>
/// river_xkb_keyboard_v1
/// <para> xkbcommon keyboard device </para>
/// <para> Version: 2 </para>
/// <see>https://wayland.app/protocols/river-xkb-config-v1/#river_xkb_keyboard_v1</see>
/// </summary>
public sealed partial class RiverXkbKeyboardV1 : WaylandObject, IWaylandObjectFactory<RiverXkbKeyboardV1>
{
    /// <summary> Wayland interface name for river_xkb_keyboard_v1. </summary>
    public const string InterfaceName = "river_xkb_keyboard_v1";
    /// <summary> Static interface name used by <see cref="IWaylandObjectFactory{T}"/>. </summary>
    public static string _StaticInterfaceName => "river_xkb_keyboard_v1";
    /// <summary> Interface version supported by this binding. </summary>
    public const int InterfaceVersion = 2;

    private bool disposed;

    private GCHandle gcHandle;
    private bool dispatcherRegistered = false;
    private readonly object dispatcherLock = new object();

    /// <summary> The display connection that owns this object. </summary>
    public new WlDisplay Display { get; private set; }

    /// <summary>
    /// Wraps an existing river_xkb_keyboard_v1 proxy handle.
    /// </summary>
    /// <param name="handle">The native Wayland proxy handle.</param>
    /// <param name="display">The display connection that owns this object.</param>
    public RiverXkbKeyboardV1(IntPtr handle, WlDisplay display)
    {
        Display = display;
        Handle = handle;
    }
    /// <summary>  </summary>
    public enum Error : uint
    {
        /// <summary>
        ///
        /// </summary>
        InvalidKeymap = 0,
    }

    /// <summary>
    /// The xkb keyboard is removed
    /// <para>
    ///
    /// This event indicates that the xkb keyboard has been removed.
    ///
    /// The server will send no further events on this object and ignore any
    /// request (other than river_xkb_keyboard_v1.destroy) made after this event
    /// is sent. The client should destroy this object with the
    /// river_xkb_keyboard_v1.destroy request to free up resources.
    /// 
    /// </para>
    /// </summary>
    public delegate void RemovedHandler();

    private RemovedHandler? _onRemoved;

    /// <summary>
    /// The xkb keyboard is removed
    /// <para>
    ///
    /// This event indicates that the xkb keyboard has been removed.
    ///
    /// The server will send no further events on this object and ignore any
    /// request (other than river_xkb_keyboard_v1.destroy) made after this event
    /// is sent. The client should destroy this object with the
    /// river_xkb_keyboard_v1.destroy request to free up resources.
    /// 
    /// </para>
    /// </summary>
    public event RemovedHandler? OnRemoved
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onRemoved += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onRemoved -= value;
        }
    }

    /// <summary>
    /// Corresponding river input device
    /// <para>
    ///
    /// The river_input_device_v1 corresponding to this xkb keyboard. This event
    /// will always be the first event sent on the river_xkb_keyboard_v1 object,
    /// and it will be sent exactly once.
    /// 
    /// </para>
    /// </summary>
    public delegate void InputDeviceHandler(RiverInputDeviceV1 device);

    private InputDeviceHandler? _onInputDevice;

    /// <summary>
    /// Corresponding river input device
    /// <para>
    ///
    /// The river_input_device_v1 corresponding to this xkb keyboard. This event
    /// will always be the first event sent on the river_xkb_keyboard_v1 object,
    /// and it will be sent exactly once.
    /// 
    /// </para>
    /// </summary>
    public event InputDeviceHandler? OnInputDevice
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onInputDevice += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onInputDevice -= value;
        }
    }

    /// <summary>
    /// Currently active layout
    /// <para>
    ///
    /// The currently active layout index and name. The name arg may be null if
    /// the active layout does not have a name.
    ///
    /// This event is sent once when the river_xkb_keyboard_v1 is created and
    /// again whenever the layout changes.
    /// 
    /// </para>
    /// </summary>
    public delegate void LayoutHandler(uint index, string? name);

    private LayoutHandler? _onLayout;

    /// <summary>
    /// Currently active layout
    /// <para>
    ///
    /// The currently active layout index and name. The name arg may be null if
    /// the active layout does not have a name.
    ///
    /// This event is sent once when the river_xkb_keyboard_v1 is created and
    /// again whenever the layout changes.
    /// 
    /// </para>
    /// </summary>
    public event LayoutHandler? OnLayout
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onLayout += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onLayout -= value;
        }
    }

    /// <summary>
    /// Capslock is currently enabled
    /// <para>
    ///
    /// Capslock is currently enabled for the keyboard.
    ///
    /// This event is sent once when the river_xkb_keyboard_v1 is created and
    /// again whenever the capslock state changes.
    /// 
    /// </para>
    /// </summary>
    public delegate void CapslockEnabledHandler();

    private CapslockEnabledHandler? _onCapslockEnabled;

    /// <summary>
    /// Capslock is currently enabled
    /// <para>
    ///
    /// Capslock is currently enabled for the keyboard.
    ///
    /// This event is sent once when the river_xkb_keyboard_v1 is created and
    /// again whenever the capslock state changes.
    /// 
    /// </para>
    /// </summary>
    public event CapslockEnabledHandler? OnCapslockEnabled
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onCapslockEnabled += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onCapslockEnabled -= value;
        }
    }

    /// <summary>
    /// Capslock is currently disabled
    /// <para>
    ///
    /// Capslock is currently disabled for the keyboard.
    ///
    /// This event is sent once when the river_xkb_keyboard_v1 is created and
    /// again whenever the capslock state changes.
    /// 
    /// </para>
    /// </summary>
    public delegate void CapslockDisabledHandler();

    private CapslockDisabledHandler? _onCapslockDisabled;

    /// <summary>
    /// Capslock is currently disabled
    /// <para>
    ///
    /// Capslock is currently disabled for the keyboard.
    ///
    /// This event is sent once when the river_xkb_keyboard_v1 is created and
    /// again whenever the capslock state changes.
    /// 
    /// </para>
    /// </summary>
    public event CapslockDisabledHandler? OnCapslockDisabled
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onCapslockDisabled += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onCapslockDisabled -= value;
        }
    }

    /// <summary>
    /// Numlock is currently enabled
    /// <para>
    ///
    /// Numlock is currently enabled for the keyboard.
    ///
    /// This event is sent once when the river_xkb_keyboard_v1 is created and
    /// again whenever the numlock state changes.
    /// 
    /// </para>
    /// </summary>
    public delegate void NumlockEnabledHandler();

    private NumlockEnabledHandler? _onNumlockEnabled;

    /// <summary>
    /// Numlock is currently enabled
    /// <para>
    ///
    /// Numlock is currently enabled for the keyboard.
    ///
    /// This event is sent once when the river_xkb_keyboard_v1 is created and
    /// again whenever the numlock state changes.
    /// 
    /// </para>
    /// </summary>
    public event NumlockEnabledHandler? OnNumlockEnabled
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onNumlockEnabled += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onNumlockEnabled -= value;
        }
    }

    /// <summary>
    /// Numlock is currently disabled
    /// <para>
    ///
    /// Numlock is currently disabled for the keyboard.
    ///
    /// This event is sent once when the river_xkb_keyboard_v1 is created and
    /// again whenever the numlock state changes.
    /// 
    /// </para>
    /// </summary>
    public delegate void NumlockDisabledHandler();

    private NumlockDisabledHandler? _onNumlockDisabled;

    /// <summary>
    /// Numlock is currently disabled
    /// <para>
    ///
    /// Numlock is currently disabled for the keyboard.
    ///
    /// This event is sent once when the river_xkb_keyboard_v1 is created and
    /// again whenever the numlock state changes.
    /// 
    /// </para>
    /// </summary>
    public event NumlockDisabledHandler? OnNumlockDisabled
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onNumlockDisabled += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onNumlockDisabled -= value;
        }
    }

    /// <summary>
    /// All information has been sent
    /// <para>
    ///
    /// This event is sent after all information about the keyboard has been
    /// sent.
    ///
    /// This allows changes to one or more river_xkb_keyboard_v1 properties to
    /// be seen as atomic, even if they happen via multiple events.
    /// 
    /// </para>
    /// </summary>
    public delegate void DoneHandler();

    private DoneHandler? _onDone;

    /// <summary>
    /// All information has been sent
    /// <para>
    ///
    /// This event is sent after all information about the keyboard has been
    /// sent.
    ///
    /// This allows changes to one or more river_xkb_keyboard_v1 properties to
    /// be seen as atomic, even if they happen via multiple events.
    /// 
    /// </para>
    /// </summary>
    public event DoneHandler? OnDone
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onDone += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onDone -= value;
        }
    }

    private unsafe void EnsureDispatcherRegistered()
    {
        lock (dispatcherLock)
        {
            if (dispatcherRegistered)
            {
                return;
            }

            // Allocate GCHandle to keep this object alive
            gcHandle = GCHandle.Alloc(this, GCHandleType.Normal);

            // Register the dispatcher with the Wayland proxy
            var dataPtr = GCHandle.ToIntPtr(gcHandle);
            var result = WaylandNative.ProxyAddDispatcher(Handle, &DispatchEvent, dataPtr, IntPtr.Zero);
            if (result != 0)
            {
                gcHandle.Free();
                throw new InvalidOperationException("Failed to add dispatcher to river_xkb_keyboard_v1");
            }

            dispatcherRegistered = true;
        }
    }

    [UnmanagedCallersOnly]
    private static unsafe int DispatchEvent(IntPtr userData, IntPtr target, uint opcode, WlMessage* message, WlArgument* args)
    {
        try
        {
            var handle = GCHandle.FromIntPtr(userData);
            var obj = (RiverXkbKeyboardV1)handle.Target!;

            switch (opcode)
            {
                case 0: // removed
                    if (obj._onRemoved != null)
                    {
                        obj._onRemoved?.Invoke();
                    }
                    break;
                case 1: // input_device
                    if (obj._onInputDevice != null)
                    {
                        if (args[0].o == (WlObject*)IntPtr.Zero) throw new InvalidOperationException("Received null object for non-nullable argument 'device'");
                        var _device = new RiverInputDeviceV1((IntPtr)args[0].o, obj.Display!);
                        obj._onInputDevice?.Invoke(_device);
                    }
                    break;
                case 2: // layout
                    if (obj._onLayout != null)
                    {
                        var _index = args[0].u;
                        var _name = Utf8StringMarshaller.ConvertToManaged(args[1].s) ?? string.Empty;
                        obj._onLayout?.Invoke(_index, _name);
                    }
                    break;
                case 3: // capslock_enabled
                    if (obj._onCapslockEnabled != null)
                    {
                        obj._onCapslockEnabled?.Invoke();
                    }
                    break;
                case 4: // capslock_disabled
                    if (obj._onCapslockDisabled != null)
                    {
                        obj._onCapslockDisabled?.Invoke();
                    }
                    break;
                case 5: // numlock_enabled
                    if (obj._onNumlockEnabled != null)
                    {
                        obj._onNumlockEnabled?.Invoke();
                    }
                    break;
                case 6: // numlock_disabled
                    if (obj._onNumlockDisabled != null)
                    {
                        obj._onNumlockDisabled?.Invoke();
                    }
                    break;
                case 7: // done
                    if (obj._onDone != null)
                    {
                        obj._onDone?.Invoke();
                    }
                    break;
                default:
                    return -1;
            }

            return 0;
        }
        catch (Exception ex)
        {
            Console.Error.WriteLine($"Error in event dispatcher: {ex}");
            return -1;
        }
    }
    /// <summary>
    /// Destroy the xkb keyboard object
    /// <para>
    /// <br/>
    /// This request indicates that the client will no longer use the keyboard<br/>
    /// object and that it may be safely destroyed.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void Destroy()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 0;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
        disposed = true;
    }

    /// <summary>
    /// Set the keymap
    /// <para>
    /// <br/>
    /// Set the keymap for the keyboard.<br/>
    /// <br/>
    /// Setting a keymap will reset all layout/modifier state.<br/>
    /// <br/>
    /// It is a protocol error to pass a keymap object for which the<br/>
    /// river_xkb_keymap_v1.success event was not received.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void SetKeymap(RiverXkbKeymapV1 keymap)
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[1];
        args[0].o = (WlObject*)(keymap?.Handle ?? IntPtr.Zero);

        const uint opcode = 1;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Set the active layout by index
    /// <para>
    /// <br/>
    /// Set the active layout for the keyboard's keymap. Has no effect if the<br/>
    /// layout index is out of bounds for the current keymap.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void SetLayoutByIndex(int index)
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[1];
        args[0].i = index;

        const uint opcode = 2;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Set the active layout by name
    /// <para>
    /// <br/>
    /// Set the active layout for the keyboard's keymap. Has no effect if there<br/>
    /// is no layout with the give name for the keyboard's keymap.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void SetLayoutByName(string name)
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[1];
        args[0].s = Utf8StringMarshaller.ConvertToUnmanaged(name);

        const uint opcode = 3;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Enable capslock
    /// <para>
    /// <br/>
    /// Enable capslock for the keyboard.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void CapslockEnable()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 4;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Disable capslock
    /// <para>
    /// <br/>
    /// Disable capslock for the keyboard.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void CapslockDisable()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 5;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Enable numlock
    /// <para>
    /// <br/>
    /// Enable numlock for the keyboard.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void NumlockEnable()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 6;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Disable numlock
    /// <para>
    /// <br/>
    /// Disable numlock for the keyboard.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void NumlockDisable()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 7;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary> Creates a RiverXkbKeyboardV1 wrapper from an existing proxy handle. </summary>
    /// <param name="handle">The native Wayland proxy handle.</param>
    /// <param name="display">The display connection that owns this object, when required.</param>
    /// <returns>A new RiverXkbKeyboardV1 instance.</returns>
    public static RiverXkbKeyboardV1 Create(nint handle, WlDisplay? display = null)
    {
        ArgumentNullException.ThrowIfNull(display);
        return new RiverXkbKeyboardV1(handle, display);
    }
}
