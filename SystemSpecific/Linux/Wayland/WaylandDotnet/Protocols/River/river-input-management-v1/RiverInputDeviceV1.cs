// <auto-generated>
// This file was generated from river-input-management-v1.xml
// Stability: River
// Link: https://wayland.app/protocols/river-input-management-v1
// </auto-generated>

#nullable enable

namespace WaylandDotnet.River;

using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;
using System.Runtime.InteropServices.Marshalling;
using System.Text;
using WaylandDotnet;
using WaylandDotnet.Internal;
using WaylandDotnet.Stable;
using WaylandDotnet.Staging;
using WaylandDotnet.Unstable;
using WaylandDotnet.Wlr;

/// <summary>
/// river_input_device_v1
/// <para> an input device </para>
/// <para> Version: 2 </para>
/// <see>https://wayland.app/protocols/river-input-management-v1/#river_input_device_v1</see>
/// </summary>
public sealed partial class RiverInputDeviceV1 : WaylandObject, IWaylandObjectFactory<RiverInputDeviceV1>
{
    /// <summary> Wayland interface name for river_input_device_v1. </summary>
    public const string InterfaceName = "river_input_device_v1";
    /// <summary> Static interface name used by <see cref="IWaylandObjectFactory{T}"/>. </summary>
    public static string _StaticInterfaceName => "river_input_device_v1";
    /// <summary> Interface version supported by this binding. </summary>
    public const int InterfaceVersion = 2;

    private bool disposed;

    private GCHandle gcHandle;
    private bool dispatcherRegistered = false;
    private readonly object dispatcherLock = new object();

    /// <summary> The display connection that owns this object. </summary>
    public new WlDisplay Display { get; private set; }

    /// <summary>
    /// Wraps an existing river_input_device_v1 proxy handle.
    /// </summary>
    /// <param name="handle">The native Wayland proxy handle.</param>
    /// <param name="display">The display connection that owns this object.</param>
    public RiverInputDeviceV1(IntPtr handle, WlDisplay display)
    {
        Display = display;
        Handle = handle;
    }
    /// <summary>  </summary>
    public enum Error : uint
    {
        /// <summary>
        ///
        /// </summary>
        InvalidRepeatInfo = 0,
        /// <summary>
        ///
        /// </summary>
        InvalidScrollFactor = 1,
        /// <summary>
        ///
        /// </summary>
        InvalidMapToRectangle = 2,
    }

    /// <summary>  </summary>
    public enum Type : uint
    {
        /// <summary>
        ///
        /// </summary>
        Keyboard = 0,
        /// <summary>
        ///
        /// </summary>
        Pointer = 1,
        /// <summary>
        ///
        /// </summary>
        Touch = 2,
        /// <summary>
        ///
        /// </summary>
        Tablet = 3,
    }

    /// <summary>
    /// The input device is removed
    /// <para>
    ///
    /// This event indicates that the input device has been removed.
    ///
    /// The server will send no further events on this object and ignore any
    /// request (other than river_input_device_v1.destroy) made after this event is
    /// sent. The client should destroy this object with the
    /// river_input_device_v1.destroy request to free up resources.
    /// 
    /// </para>
    /// </summary>
    public delegate void RemovedHandler();

    private RemovedHandler? _onRemoved;

    /// <summary>
    /// The input device is removed
    /// <para>
    ///
    /// This event indicates that the input device has been removed.
    ///
    /// The server will send no further events on this object and ignore any
    /// request (other than river_input_device_v1.destroy) made after this event is
    /// sent. The client should destroy this object with the
    /// river_input_device_v1.destroy request to free up resources.
    /// 
    /// </para>
    /// </summary>
    public event RemovedHandler? OnRemoved
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onRemoved += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onRemoved -= value;
        }
    }

    /// <summary>
    /// The type of the input device
    /// <para>
    ///
    /// The type of the input device. This event is sent once when the
    /// river_input_device_v1 object is created. The device type cannot
    /// change during the lifetime of the object.
    /// 
    /// </para>
    /// </summary>
    public delegate void TypeHandler(uint type);

    private TypeHandler? _onType;

    /// <summary>
    /// The type of the input device
    /// <para>
    ///
    /// The type of the input device. This event is sent once when the
    /// river_input_device_v1 object is created. The device type cannot
    /// change during the lifetime of the object.
    /// 
    /// </para>
    /// </summary>
    public event TypeHandler? OnType
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onType += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onType -= value;
        }
    }

    /// <summary>
    /// The name of the input device
    /// <para>
    ///
    /// The name of the input device. This event is sent once when the
    /// river_input_device_v1 object is created. The device name cannot
    /// change during the lifetime of the object.
    /// 
    /// </para>
    /// </summary>
    public delegate void NameHandler(string name);

    private NameHandler? _onName;

    /// <summary>
    /// The name of the input device
    /// <para>
    ///
    /// The name of the input device. This event is sent once when the
    /// river_input_device_v1 object is created. The device name cannot
    /// change during the lifetime of the object.
    /// 
    /// </para>
    /// </summary>
    public event NameHandler? OnName
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onName += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onName -= value;
        }
    }

    /// <summary>
    /// All information has been sent
    /// <para>
    ///
    /// This event is sent after all information about the input device has
    /// been sent.
    ///
    /// This allows changes to one or more river_input_device_v1 properties to
    /// be seen as atomic, even if they happen via multiple events.
    /// 
    /// </para>
    /// </summary>
    public delegate void DoneHandler();

    private DoneHandler? _onDone;

    /// <summary>
    /// All information has been sent
    /// <para>
    ///
    /// This event is sent after all information about the input device has
    /// been sent.
    ///
    /// This allows changes to one or more river_input_device_v1 properties to
    /// be seen as atomic, even if they happen via multiple events.
    /// 
    /// </para>
    /// </summary>
    public event DoneHandler? OnDone
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onDone += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onDone -= value;
        }
    }

    private unsafe void EnsureDispatcherRegistered()
    {
        lock (dispatcherLock)
        {
            if (dispatcherRegistered)
            {
                return;
            }

            // Allocate GCHandle to keep this object alive
            gcHandle = GCHandle.Alloc(this, GCHandleType.Normal);

            // Register the dispatcher with the Wayland proxy
            var dataPtr = GCHandle.ToIntPtr(gcHandle);
            var result = WaylandNative.ProxyAddDispatcher(Handle, &DispatchEvent, dataPtr, IntPtr.Zero);
            if (result != 0)
            {
                gcHandle.Free();
                throw new InvalidOperationException("Failed to add dispatcher to river_input_device_v1");
            }

            dispatcherRegistered = true;
        }
    }

    [UnmanagedCallersOnly]
    private static unsafe int DispatchEvent(IntPtr userData, IntPtr target, uint opcode, WlMessage* message, WlArgument* args)
    {
        try
        {
            var handle = GCHandle.FromIntPtr(userData);
            var obj = (RiverInputDeviceV1)handle.Target!;

            switch (opcode)
            {
                case 0: // removed
                    if (obj._onRemoved != null)
                    {
                        obj._onRemoved?.Invoke();
                    }
                    break;
                case 1: // type
                    if (obj._onType != null)
                    {
                        var _type = args[0].u;
                        obj._onType?.Invoke(_type);
                    }
                    break;
                case 2: // name
                    if (obj._onName != null)
                    {
                        var _name = Utf8StringMarshaller.ConvertToManaged(args[0].s) ?? string.Empty;
                        obj._onName?.Invoke(_name);
                    }
                    break;
                case 3: // done
                    if (obj._onDone != null)
                    {
                        obj._onDone?.Invoke();
                    }
                    break;
                default:
                    return -1;
            }

            return 0;
        }
        catch (Exception ex)
        {
            Console.Error.WriteLine($"Error in event dispatcher: {ex}");
            return -1;
        }
    }
    /// <summary>
    /// Destroy the input device object
    /// <para>
    /// <br/>
    /// This request indicates that the client will no longer use the input<br/>
    /// device object and that it may be safely destroyed.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void Destroy()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 0;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
        disposed = true;
    }

    /// <summary>
    /// Assign the input device to a seat
    /// <para>
    /// <br/>
    /// Assign the input device to a seat. All input devices not explicitly<br/>
    /// assigned to a seat are considered assigned to the default seat.<br/>
    /// <br/>
    /// Has no effect if a seat with the given name does not exist.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void AssignToSeat(string name)
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[1];
        args[0].s = Utf8StringMarshaller.ConvertToUnmanaged(name);

        const uint opcode = 1;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Set keyboard repeat rate and delay
    /// <para>
    /// <br/>
    /// Set repeat rate and delay for a keyboard input device. Has no effect if<br/>
    /// the device is not a keyboard.<br/>
    /// <br/>
    /// Negative values for either rate or delay are illegal. A rate of zero<br/>
    /// will disable any repeating (regardless of the value of delay).<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void SetRepeatInfo(int rate, int delay)
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[2];
        args[0].i = rate;
        args[1].i = delay;

        const uint opcode = 2;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Set scroll factor
    /// <para>
    /// <br/>
    /// Set the scroll factor for a pointer input device. Has no effect if the<br/>
    /// device is not a pointer.<br/>
    /// <br/>
    /// For example, a factor of 0.5 will make scrolling twice as slow while a<br/>
    /// factor of 3.0 will make scrolling 3 times as fast.<br/>
    /// <br/>
    /// Setting a scroll factor less than 0 is a protocol error.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void SetScrollFactor(WlFixed factor)
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[1];
        args[0].f = factor;

        const uint opcode = 3;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Map input device to the given output
    /// <para>
    /// <br/>
    /// Map the input device to the given output. Has no effect if the device is<br/>
    /// not a pointer, touch, or tablet device.<br/>
    /// <br/>
    /// If mapped to both an output and a rectangle, the rectangle has priority.<br/>
    /// <br/>
    /// Passing null clears an existing mapping.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void MapToOutput(WlOutput? output)
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[1];
        args[0].o = (WlObject*)(output?.Handle ?? IntPtr.Zero);

        const uint opcode = 4;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Map input device to the given rectangle
    /// <para>
    /// <br/>
    /// Map the input device to the given rectangle in the global compositor<br/>
    /// coordinate space. Has no effect if the device is not a pointer, touch,<br/>
    /// or tablet device.<br/>
    /// <br/>
    /// If mapped to both an output and a rectangle, the rectangle has priority.<br/>
    /// <br/>
    /// Width and height must be greater than or equal to 0.<br/>
    /// <br/>
    /// Passing 0 for width or height clears an existing mapping.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void MapToRectangle(int x, int y, int width, int height)
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[4];
        args[0].i = x;
        args[1].i = y;
        args[2].i = width;
        args[3].i = height;

        const uint opcode = 5;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary> Creates a RiverInputDeviceV1 wrapper from an existing proxy handle. </summary>
    /// <param name="handle">The native Wayland proxy handle.</param>
    /// <param name="display">The display connection that owns this object, when required.</param>
    /// <returns>A new RiverInputDeviceV1 instance.</returns>
    public static RiverInputDeviceV1 Create(nint handle, WlDisplay? display = null)
    {
        ArgumentNullException.ThrowIfNull(display);
        return new RiverInputDeviceV1(handle, display);
    }
}
