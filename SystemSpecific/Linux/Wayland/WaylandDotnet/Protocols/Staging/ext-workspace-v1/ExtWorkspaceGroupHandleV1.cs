// <auto-generated>
// This file was generated from ext-workspace-v1.xml
// Stability: Staging
// Link: https://wayland.app/protocols/ext-workspace-v1
// </auto-generated>

#nullable enable

namespace WaylandDotnet.Staging;

using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;
using System.Runtime.InteropServices.Marshalling;
using System.Text;
using WaylandDotnet;
using WaylandDotnet.Internal;
using WaylandDotnet.Stable;
using WaylandDotnet.Staging;
using WaylandDotnet.Unstable;
using WaylandDotnet.Wlr;

/// <summary>
/// ext_workspace_group_handle_v1
/// <para> a workspace group assigned to a set of outputs </para>
/// <para> Version: 1 </para>
/// <see>https://wayland.app/protocols/ext-workspace-v1/#ext_workspace_group_handle_v1</see>
/// </summary>
public sealed partial class ExtWorkspaceGroupHandleV1 : WaylandObject, IWaylandObjectFactory<ExtWorkspaceGroupHandleV1>
{
    /// <summary> Wayland interface name for ext_workspace_group_handle_v1. </summary>
    public const string InterfaceName = "ext_workspace_group_handle_v1";
    /// <summary> Static interface name used by <see cref="IWaylandObjectFactory{T}"/>. </summary>
    public static string _StaticInterfaceName => "ext_workspace_group_handle_v1";
    /// <summary> Interface version supported by this binding. </summary>
    public const int InterfaceVersion = 1;

    private bool disposed;

    private GCHandle gcHandle;
    private bool dispatcherRegistered = false;
    private readonly object dispatcherLock = new object();

    /// <summary> The display connection that owns this object. </summary>
    public new WlDisplay Display { get; private set; }

    /// <summary>
    /// Wraps an existing ext_workspace_group_handle_v1 proxy handle.
    /// </summary>
    /// <param name="handle">The native Wayland proxy handle.</param>
    /// <param name="display">The display connection that owns this object.</param>
    public ExtWorkspaceGroupHandleV1(IntPtr handle, WlDisplay display)
    {
        Display = display;
        Handle = handle;
    }
    /// <summary>  </summary>
    [Flags]
    public enum GroupCapabilitiesFlag : uint
    {
        /// <summary>
        /// create_workspace request is available
        /// </summary>
        CreateWorkspace = 1,
    }

    /// <summary>
    /// Compositor capabilities
    /// <para>
    ///
    /// This event advertises the capabilities supported by the compositor. If
    /// a capability isn't supported, clients should hide or disable the UI
    /// elements that expose this functionality. For instance, if the
    /// compositor doesn't advertise support for creating workspaces, a button
    /// triggering the create_workspace request should not be displayed.
    ///
    /// The compositor will ignore requests it doesn't support. For instance,
    /// a compositor which doesn't advertise support for creating workspaces will ignore
    /// create_workspace requests.
    ///
    /// Compositors must send this event once after creation of an
    /// ext_workspace_group_handle_v1. When the capabilities change, compositors
    /// must send this event again.
    /// 
    /// </para>
    /// </summary>
    public delegate void CapabilitiesHandler(uint capabilities);

    private CapabilitiesHandler? _onCapabilities;

    /// <summary>
    /// Compositor capabilities
    /// <para>
    ///
    /// This event advertises the capabilities supported by the compositor. If
    /// a capability isn't supported, clients should hide or disable the UI
    /// elements that expose this functionality. For instance, if the
    /// compositor doesn't advertise support for creating workspaces, a button
    /// triggering the create_workspace request should not be displayed.
    ///
    /// The compositor will ignore requests it doesn't support. For instance,
    /// a compositor which doesn't advertise support for creating workspaces will ignore
    /// create_workspace requests.
    ///
    /// Compositors must send this event once after creation of an
    /// ext_workspace_group_handle_v1. When the capabilities change, compositors
    /// must send this event again.
    /// 
    /// </para>
    /// </summary>
    public event CapabilitiesHandler? OnCapabilities
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onCapabilities += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onCapabilities -= value;
        }
    }

    /// <summary>
    /// Output assigned to workspace group
    /// <para>
    ///
    /// This event is emitted whenever an output is assigned to the workspace
    /// group or a new `wl_output` object is bound by the client, which was already
    /// assigned to this workspace_group.
    /// 
    /// </para>
    /// </summary>
    public delegate void OutputEnterHandler(WlOutput output);

    private OutputEnterHandler? _onOutputEnter;

    /// <summary>
    /// Output assigned to workspace group
    /// <para>
    ///
    /// This event is emitted whenever an output is assigned to the workspace
    /// group or a new `wl_output` object is bound by the client, which was already
    /// assigned to this workspace_group.
    /// 
    /// </para>
    /// </summary>
    public event OutputEnterHandler? OnOutputEnter
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onOutputEnter += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onOutputEnter -= value;
        }
    }

    /// <summary>
    /// Output removed from workspace group
    /// <para>
    ///
    /// This event is emitted whenever an output is removed from the workspace
    /// group.
    /// 
    /// </para>
    /// </summary>
    public delegate void OutputLeaveHandler(WlOutput output);

    private OutputLeaveHandler? _onOutputLeave;

    /// <summary>
    /// Output removed from workspace group
    /// <para>
    ///
    /// This event is emitted whenever an output is removed from the workspace
    /// group.
    /// 
    /// </para>
    /// </summary>
    public event OutputLeaveHandler? OnOutputLeave
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onOutputLeave += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onOutputLeave -= value;
        }
    }

    /// <summary>
    /// Workspace added to workspace group
    /// <para>
    ///
    /// This event is emitted whenever a workspace is assigned to this group.
    /// A workspace may only ever be assigned to a single group at a single point
    /// in time, but can be re-assigned during its lifetime.
    /// 
    /// </para>
    /// </summary>
    public delegate void WorkspaceEnterHandler(ExtWorkspaceHandleV1 workspace);

    private WorkspaceEnterHandler? _onWorkspaceEnter;

    /// <summary>
    /// Workspace added to workspace group
    /// <para>
    ///
    /// This event is emitted whenever a workspace is assigned to this group.
    /// A workspace may only ever be assigned to a single group at a single point
    /// in time, but can be re-assigned during its lifetime.
    /// 
    /// </para>
    /// </summary>
    public event WorkspaceEnterHandler? OnWorkspaceEnter
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onWorkspaceEnter += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onWorkspaceEnter -= value;
        }
    }

    /// <summary>
    /// Workspace removed from workspace group
    /// <para>
    ///
    /// This event is emitted whenever a workspace is removed from this group.
    /// 
    /// </para>
    /// </summary>
    public delegate void WorkspaceLeaveHandler(ExtWorkspaceHandleV1 workspace);

    private WorkspaceLeaveHandler? _onWorkspaceLeave;

    /// <summary>
    /// Workspace removed from workspace group
    /// <para>
    ///
    /// This event is emitted whenever a workspace is removed from this group.
    /// 
    /// </para>
    /// </summary>
    public event WorkspaceLeaveHandler? OnWorkspaceLeave
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onWorkspaceLeave += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onWorkspaceLeave -= value;
        }
    }

    /// <summary>
    /// This workspace group has been removed
    /// <para>
    ///
    /// This event is send when the group associated with the ext_workspace_group_handle_v1
    /// has been removed. After sending this request the compositor will immediately consider
    /// the object inert. Any requests will be ignored except the destroy request.
    /// It is guaranteed there won't be any more events referencing this
    /// ext_workspace_group_handle_v1.
    ///
    /// The compositor must remove all workspaces belonging to a workspace group
    /// via a workspace_leave event before removing the workspace group.
    /// 
    /// </para>
    /// </summary>
    public delegate void RemovedHandler();

    private RemovedHandler? _onRemoved;

    /// <summary>
    /// This workspace group has been removed
    /// <para>
    ///
    /// This event is send when the group associated with the ext_workspace_group_handle_v1
    /// has been removed. After sending this request the compositor will immediately consider
    /// the object inert. Any requests will be ignored except the destroy request.
    /// It is guaranteed there won't be any more events referencing this
    /// ext_workspace_group_handle_v1.
    ///
    /// The compositor must remove all workspaces belonging to a workspace group
    /// via a workspace_leave event before removing the workspace group.
    /// 
    /// </para>
    /// </summary>
    public event RemovedHandler? OnRemoved
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onRemoved += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onRemoved -= value;
        }
    }

    private unsafe void EnsureDispatcherRegistered()
    {
        lock (dispatcherLock)
        {
            if (dispatcherRegistered)
            {
                return;
            }

            // Allocate GCHandle to keep this object alive
            gcHandle = GCHandle.Alloc(this, GCHandleType.Normal);

            // Register the dispatcher with the Wayland proxy
            var dataPtr = GCHandle.ToIntPtr(gcHandle);
            var result = WaylandNative.ProxyAddDispatcher(Handle, &DispatchEvent, dataPtr, IntPtr.Zero);
            if (result != 0)
            {
                gcHandle.Free();
                throw new InvalidOperationException("Failed to add dispatcher to ext_workspace_group_handle_v1");
            }

            dispatcherRegistered = true;
        }
    }

    [UnmanagedCallersOnly]
    private static unsafe int DispatchEvent(IntPtr userData, IntPtr target, uint opcode, WlMessage* message, WlArgument* args)
    {
        try
        {
            var handle = GCHandle.FromIntPtr(userData);
            var obj = (ExtWorkspaceGroupHandleV1)handle.Target!;

            switch (opcode)
            {
                case 0: // capabilities
                    if (obj._onCapabilities != null)
                    {
                        var _capabilities = args[0].u;
                        obj._onCapabilities?.Invoke(_capabilities);
                    }
                    break;
                case 1: // output_enter
                    if (obj._onOutputEnter != null)
                    {
                        if (args[0].o == (WlObject*)IntPtr.Zero) throw new InvalidOperationException("Received null object for non-nullable argument 'output'");
                        var _output = new WlOutput((IntPtr)args[0].o, obj.Display!);
                        obj._onOutputEnter?.Invoke(_output);
                    }
                    break;
                case 2: // output_leave
                    if (obj._onOutputLeave != null)
                    {
                        if (args[0].o == (WlObject*)IntPtr.Zero) throw new InvalidOperationException("Received null object for non-nullable argument 'output'");
                        var _output = new WlOutput((IntPtr)args[0].o, obj.Display!);
                        obj._onOutputLeave?.Invoke(_output);
                    }
                    break;
                case 3: // workspace_enter
                    if (obj._onWorkspaceEnter != null)
                    {
                        if (args[0].o == (WlObject*)IntPtr.Zero) throw new InvalidOperationException("Received null object for non-nullable argument 'workspace'");
                        var _workspace = new ExtWorkspaceHandleV1((IntPtr)args[0].o, obj.Display!);
                        obj._onWorkspaceEnter?.Invoke(_workspace);
                    }
                    break;
                case 4: // workspace_leave
                    if (obj._onWorkspaceLeave != null)
                    {
                        if (args[0].o == (WlObject*)IntPtr.Zero) throw new InvalidOperationException("Received null object for non-nullable argument 'workspace'");
                        var _workspace = new ExtWorkspaceHandleV1((IntPtr)args[0].o, obj.Display!);
                        obj._onWorkspaceLeave?.Invoke(_workspace);
                    }
                    break;
                case 5: // removed
                    if (obj._onRemoved != null)
                    {
                        obj._onRemoved?.Invoke();
                    }
                    break;
                default:
                    return -1;
            }

            return 0;
        }
        catch (Exception ex)
        {
            Console.Error.WriteLine($"Error in event dispatcher: {ex}");
            return -1;
        }
    }
    /// <summary>
    /// Create a new workspace
    /// <para>
    /// <br/>
    /// Request that the compositor create a new workspace with the given name<br/>
    /// and assign it to this group.<br/>
    /// <br/>
    /// There is no guarantee that the compositor will create a new workspace,<br/>
    /// or that the created workspace will have the provided name.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void CreateWorkspace(string workspace)
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[1];
        args[0].s = Utf8StringMarshaller.ConvertToUnmanaged(workspace);

        const uint opcode = 0;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Destroy the ext_workspace_group_handle_v1 object
    /// <para>
    /// <br/>
    /// Destroys the ext_workspace_group_handle_v1 object.<br/>
    /// <br/>
    /// This request should be send either when the client does not want to<br/>
    /// use the workspace group object any more or after the removed event to finalize<br/>
    /// the destruction of the object.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void Destroy()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 1;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
        disposed = true;
    }

    /// <summary> Creates a ExtWorkspaceGroupHandleV1 wrapper from an existing proxy handle. </summary>
    /// <param name="handle">The native Wayland proxy handle.</param>
    /// <param name="display">The display connection that owns this object, when required.</param>
    /// <returns>A new ExtWorkspaceGroupHandleV1 instance.</returns>
    public static ExtWorkspaceGroupHandleV1 Create(nint handle, WlDisplay? display = null)
    {
        ArgumentNullException.ThrowIfNull(display);
        return new ExtWorkspaceGroupHandleV1(handle, display);
    }
}
