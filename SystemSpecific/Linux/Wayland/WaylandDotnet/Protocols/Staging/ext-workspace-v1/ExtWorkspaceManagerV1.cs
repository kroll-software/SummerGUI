// <auto-generated>
// This file was generated from ext-workspace-v1.xml
// Stability: Staging
// Link: https://wayland.app/protocols/ext-workspace-v1
// </auto-generated>

#nullable enable

namespace WaylandDotnet.Staging;

using System;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;
using System.Runtime.InteropServices.Marshalling;
using System.Text;
using WaylandDotnet;
using WaylandDotnet.Internal;
using WaylandDotnet.Stable;
using WaylandDotnet.Staging;
using WaylandDotnet.Unstable;
using WaylandDotnet.Wlr;

/// <summary>
/// ext_workspace_manager_v1
/// <para> list and control workspaces </para>
/// <para> Version: 1 </para>
/// <see>https://wayland.app/protocols/ext-workspace-v1/#ext_workspace_manager_v1</see>
/// </summary>
public sealed partial class ExtWorkspaceManagerV1 : WaylandObject, IWaylandObjectFactory<ExtWorkspaceManagerV1>
{
    /// <summary> Wayland interface name for ext_workspace_manager_v1. </summary>
    public const string InterfaceName = "ext_workspace_manager_v1";
    /// <summary> Static interface name used by <see cref="IWaylandObjectFactory{T}"/>. </summary>
    public static string _StaticInterfaceName => "ext_workspace_manager_v1";
    /// <summary> Interface version supported by this binding. </summary>
    public const int InterfaceVersion = 1;

    private bool disposed;

    private GCHandle gcHandle;
    private bool dispatcherRegistered = false;
    private readonly object dispatcherLock = new object();

    /// <summary> The display connection that owns this object. </summary>
    public new WlDisplay Display { get; private set; }

    /// <summary>
    /// Wraps an existing ext_workspace_manager_v1 proxy handle.
    /// </summary>
    /// <param name="handle">The native Wayland proxy handle.</param>
    /// <param name="display">The display connection that owns this object.</param>
    public ExtWorkspaceManagerV1(IntPtr handle, WlDisplay display)
    {
        Display = display;
        Handle = handle;
    }
    /// <summary>
    /// A workspace group has been created
    /// <para>
    ///
    /// This event is emitted whenever a new workspace group has been created.
    ///
    /// All initial details of the workspace group (outputs) will be
    /// sent immediately after this event via the corresponding events in
    /// ext_workspace_group_handle_v1 and ext_workspace_handle_v1.
    /// 
    /// </para>
    /// </summary>
    public delegate void WorkspaceGroupHandler(ExtWorkspaceGroupHandleV1 workspaceGroup);

    private WorkspaceGroupHandler? _onWorkspaceGroup;

    /// <summary>
    /// A workspace group has been created
    /// <para>
    ///
    /// This event is emitted whenever a new workspace group has been created.
    ///
    /// All initial details of the workspace group (outputs) will be
    /// sent immediately after this event via the corresponding events in
    /// ext_workspace_group_handle_v1 and ext_workspace_handle_v1.
    /// 
    /// </para>
    /// </summary>
    public event WorkspaceGroupHandler? OnWorkspaceGroup
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onWorkspaceGroup += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onWorkspaceGroup -= value;
        }
    }

    /// <summary>
    /// Workspace has been created
    /// <para>
    ///
    /// This event is emitted whenever a new workspace has been created.
    ///
    /// All initial details of the workspace (name, coordinates, state) will
    /// be sent immediately after this event via the corresponding events in
    /// ext_workspace_handle_v1.
    ///
    /// Workspaces start off unassigned to any workspace group.
    /// 
    /// </para>
    /// </summary>
    public delegate void WorkspaceHandler(ExtWorkspaceHandleV1 workspace);

    private WorkspaceHandler? _onWorkspace;

    /// <summary>
    /// Workspace has been created
    /// <para>
    ///
    /// This event is emitted whenever a new workspace has been created.
    ///
    /// All initial details of the workspace (name, coordinates, state) will
    /// be sent immediately after this event via the corresponding events in
    /// ext_workspace_handle_v1.
    ///
    /// Workspaces start off unassigned to any workspace group.
    /// 
    /// </para>
    /// </summary>
    public event WorkspaceHandler? OnWorkspace
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onWorkspace += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onWorkspace -= value;
        }
    }

    /// <summary>
    /// All information about the workspaces and workspace groups has been sent
    /// <para>
    ///
    /// This event is sent after all changes in all workspaces and workspace groups have been
    /// sent.
    ///
    /// This allows changes to one or more ext_workspace_group_handle_v1
    /// properties and ext_workspace_handle_v1 properties
    /// to be seen as atomic, even if they happen via multiple events.
    /// In particular, an output moving from one workspace group to
    /// another sends an output_enter event and an output_leave event to the two
    /// ext_workspace_group_handle_v1 objects in question. The compositor sends
    /// the done event only after updating the output information in both
    /// workspace groups.
    /// 
    /// </para>
    /// </summary>
    public delegate void DoneHandler();

    private DoneHandler? _onDone;

    /// <summary>
    /// All information about the workspaces and workspace groups has been sent
    /// <para>
    ///
    /// This event is sent after all changes in all workspaces and workspace groups have been
    /// sent.
    ///
    /// This allows changes to one or more ext_workspace_group_handle_v1
    /// properties and ext_workspace_handle_v1 properties
    /// to be seen as atomic, even if they happen via multiple events.
    /// In particular, an output moving from one workspace group to
    /// another sends an output_enter event and an output_leave event to the two
    /// ext_workspace_group_handle_v1 objects in question. The compositor sends
    /// the done event only after updating the output information in both
    /// workspace groups.
    /// 
    /// </para>
    /// </summary>
    public event DoneHandler? OnDone
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onDone += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onDone -= value;
        }
    }

    /// <summary>
    /// The compositor has finished with the workspace_manager
    /// <para>
    ///
    /// This event indicates that the compositor is done sending events to the
    /// ext_workspace_manager_v1. The server will destroy the object
    /// immediately after sending this request.
    /// 
    /// </para>
    /// </summary>
    public delegate void FinishedHandler();

    private FinishedHandler? _onFinished;

    /// <summary>
    /// The compositor has finished with the workspace_manager
    /// <para>
    ///
    /// This event indicates that the compositor is done sending events to the
    /// ext_workspace_manager_v1. The server will destroy the object
    /// immediately after sending this request.
    /// 
    /// </para>
    /// </summary>
    public event FinishedHandler? OnFinished
    {
        add
        {
            ObjectDisposedException.ThrowIf(disposed, this);
            _onFinished += value;
            EnsureDispatcherRegistered();
        }

        remove
        {
            _onFinished -= value;
        }
    }

    private unsafe void EnsureDispatcherRegistered()
    {
        lock (dispatcherLock)
        {
            if (dispatcherRegistered)
            {
                return;
            }

            // Allocate GCHandle to keep this object alive
            gcHandle = GCHandle.Alloc(this, GCHandleType.Normal);

            // Register the dispatcher with the Wayland proxy
            var dataPtr = GCHandle.ToIntPtr(gcHandle);
            var result = WaylandNative.ProxyAddDispatcher(Handle, &DispatchEvent, dataPtr, IntPtr.Zero);
            if (result != 0)
            {
                gcHandle.Free();
                throw new InvalidOperationException("Failed to add dispatcher to ext_workspace_manager_v1");
            }

            dispatcherRegistered = true;
        }
    }

    [UnmanagedCallersOnly]
    private static unsafe int DispatchEvent(IntPtr userData, IntPtr target, uint opcode, WlMessage* message, WlArgument* args)
    {
        try
        {
            var handle = GCHandle.FromIntPtr(userData);
            var obj = (ExtWorkspaceManagerV1)handle.Target!;

            switch (opcode)
            {
                case 0: // workspace_group
                    if (obj._onWorkspaceGroup != null)
                    {
                        if (args[0].o == (WlObject*)IntPtr.Zero) throw new InvalidOperationException("Received null object for non-nullable argument 'workspace_group'");
                        var _workspaceGroup = new ExtWorkspaceGroupHandleV1((IntPtr)args[0].o, obj.Display!);
                        obj._onWorkspaceGroup?.Invoke(_workspaceGroup);
                    }
                    break;
                case 1: // workspace
                    if (obj._onWorkspace != null)
                    {
                        if (args[0].o == (WlObject*)IntPtr.Zero) throw new InvalidOperationException("Received null object for non-nullable argument 'workspace'");
                        var _workspace = new ExtWorkspaceHandleV1((IntPtr)args[0].o, obj.Display!);
                        obj._onWorkspace?.Invoke(_workspace);
                    }
                    break;
                case 2: // done
                    if (obj._onDone != null)
                    {
                        obj._onDone?.Invoke();
                    }
                    break;
                case 3: // finished
                    if (obj._onFinished != null)
                    {
                        obj._onFinished?.Invoke();
                        obj.disposed = true;
                    }
                    break;
                default:
                    return -1;
            }

            return 0;
        }
        catch (Exception ex)
        {
            Console.Error.WriteLine($"Error in event dispatcher: {ex}");
            return -1;
        }
    }
    /// <summary>
    /// All requests about the workspaces have been sent
    /// <para>
    /// <br/>
    /// The client must send this request after it has finished sending other<br/>
    /// requests. The compositor must process a series of requests preceding a<br/>
    /// commit request atomically.<br/>
    /// <br/>
    /// This allows changes to the workspace properties to be seen as atomic,<br/>
    /// even if they happen via multiple events, and even if they involve<br/>
    /// multiple ext_workspace_handle_v1 objects, for example, deactivating one<br/>
    /// workspace and activating another.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void Commit()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 0;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary>
    /// Stop sending events
    /// <para>
    /// <br/>
    /// Indicates the client no longer wishes to receive events for new<br/>
    /// workspace groups. However the compositor may emit further workspace<br/>
    /// events, until the finished event is emitted. The compositor is expected<br/>
    /// to send the finished event eventually once the stop request has been processed.<br/>
    /// <br/>
    /// The client must not send any requests after this one, doing so will raise a wl_display<br/>
    /// invalid_object error.<br/>
    /// <br/>
    /// </para>
    /// </summary>
    public unsafe void Stop()
    {
        ObjectDisposedException.ThrowIf(disposed, this);

        var args = stackalloc WlArgument[0];

        const uint opcode = 1;

        var newProxy = WaylandNative.ProxyMarshalArrayFlags(
            Handle,
            opcode,
            (WlInterface*)IntPtr.Zero,
            0,
            0,
            (nint)args
        );
    }

    /// <summary> Creates a ExtWorkspaceManagerV1 wrapper from an existing proxy handle. </summary>
    /// <param name="handle">The native Wayland proxy handle.</param>
    /// <param name="display">The display connection that owns this object, when required.</param>
    /// <returns>A new ExtWorkspaceManagerV1 instance.</returns>
    public static ExtWorkspaceManagerV1 Create(nint handle, WlDisplay? display = null)
    {
        ArgumentNullException.ThrowIfNull(display);
        return new ExtWorkspaceManagerV1(handle, display);
    }
}
